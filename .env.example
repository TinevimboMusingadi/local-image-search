# Path to GCP service account JSON (e.g. key/rich-phenomenon-429412-t6-97496af05d96.json)
GOOGLE_APPLICATION_CREDENTIALS=key/your-service-account.json

# GCP project ID (from your service account key)
GCP_PROJECT_ID=your-project-id

# Vertex AI region (e.g. us-central1)
GCP_LOCATION=us-central1

# ChromaDB persistent storage directory
CHROMA_PERSIST_DIR=./chroma_data

# Optional: base path for indexing/serving images (default: project root)
# IMAGE_BASE_PATH=.
